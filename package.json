{
  "name": "snmp",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "ts-node-dev --inspect --respawn --poll --transpile-only --ignore-watch node_modules index.ts"
  },
  "dependencies": {
    "net-snmp": "^3.5.2"
  },
  "devDependencies": {
    "@types/node": "^16.4.10",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.3.5"
  }
}
